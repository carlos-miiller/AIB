<Window x:Class="AIB.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurações AIB" 
        Height="650" Width="450"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="#01000000" 
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <SolidColorBrush x:Key="FlyoutBg" Color="#EE121214" />
        <SolidColorBrush x:Key="SystemBorder" Color="#3FFFFFFF" />
        <SolidColorBrush x:Key="SystemText" Color="#F2F2F5" />
        <SolidColorBrush x:Key="InputBg" Color="#15FFFFFF" />
        
        <LinearGradientBrush x:Key="AiAccent" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#2B5BFF" Offset="0"/>
            <GradientStop Color="#6A3BFF" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SystemText}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,12,0,4"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2A2A30"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style x:Key="BrowseButton" TargetType="Button">
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2A2A30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Sombra de flutuação -->
    <Window.Effect>
        <DropShadowEffect BlurRadius="28" ShadowDepth="6" Direction="270"
                          Color="#000000" Opacity="0.55"/>
    </Window.Effect>

    <!-- Container principal com bordas arredondadas e gradiente -->
    <Border CornerRadius="16" Padding="2">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#9B51E0" Offset="0"/>
                <GradientStop Color="#3182CE" Offset="0.33"/>
                <GradientStop Color="#D53F8C" Offset="0.66"/>
                <GradientStop Color="#E28743" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <!-- Fundo Escuro Interno -->
        <Border CornerRadius="14" Background="#F01A1A1E">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="CONFIGURAÇÕES" FontSize="18" FontWeight="Bold" Margin="0,0,0,5" Foreground="White"/>
                <TextBlock Text="Personalize a conexão e o comportamento do AIB." Margin="0,0,0,10" Opacity="0.6"/>
                <Border Height="1" Background="{StaticResource AiAccent}" Opacity="0.5" Margin="0,0,0,10"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel VerticalAlignment="Top" Margin="0,0,10,0">
                    <TextBlock Text="CONEXÃO" FontWeight="Bold" Opacity="0.8" Margin="0,5,0,5"/>
                    
                    <TextBlock Text="Provedor de IA"/>
                    <ComboBox x:Name="ProviderComboBox" Height="38" SelectionChanged="ProviderComboBox_SelectionChanged">
                        <ComboBoxItem Content="Ollama"/>
                        <ComboBoxItem Content="Google Gemini"/>
                    </ComboBox>

                    <StackPanel x:Name="AdvancedConnectionPanel">
                        <TextBlock Text="Base URL (Ollama ou OpenAI)"/>
                        <TextBox x:Name="UrlTextBox" Text="http://localhost:11434/v1" LostFocus="UrlTextBox_LostFocus"/>

                        <TextBlock Text="API Key"/>
                        <TextBox x:Name="KeyTextBox" Text="ollama"/>

                        <TextBlock Text="Modelo LLM"/>
                        <Grid>
                            <ComboBox x:Name="ModelComboBox" IsEditable="True" Height="38" VerticalContentAlignment="Center"/>
                            <ProgressBar x:Name="LoadingProgress" IsIndeterminate="True" Height="2" VerticalAlignment="Bottom" Visibility="Collapsed"/>
                        </Grid>
                    </StackPanel>

                    <Separator Background="White" Opacity="0.1" Margin="0,20,0,10"/>
                    <TextBlock Text="DIRETÓRIOS" FontWeight="Bold" Opacity="0.8" Margin="0,5,0,5"/>

                    <TextBlock Text="Diretório de Dados"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="DataDirTextBox" Grid.Column="0" IsReadOnly="True" Opacity="0.7"/>
                        <Button Content="📁" Grid.Column="1" Margin="5,0,0,0" Style="{StaticResource BrowseButton}" Click="BrowseDataDir_Click"/>
                    </Grid>
                    <TextBlock Text="Skills, memória e configurações persistentes." FontSize="11" Opacity="0.4" Margin="0,2,0,0"/>

                    <TextBlock Text="Diretório Temporário"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TempDirTextBox" Grid.Column="0" IsReadOnly="True" Opacity="0.7"/>
                        <Button Content="📁" Grid.Column="1" Margin="5,0,0,0" Style="{StaticResource BrowseButton}" Click="BrowseTempDir_Click"/>
                    </Grid>
                    <TextBlock Text="Cache de OCR e arquivos temporários da IA." FontSize="11" Opacity="0.4" Margin="0,2,0,0"/>

                    <Separator Background="White" Opacity="0.1" Margin="0,20,0,10"/>
                    <TextBlock Text="AVANÇADO" FontWeight="Bold" Opacity="0.8" Margin="0,5,0,5"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Máximo de Tokens de Contexto" Grid.Column="0" VerticalAlignment="Center" ToolTip="Determinado automaticamente pelo seu Nível"/>
                        <TextBox x:Name="MaxHistoryTextBox" Grid.Column="1" Width="70" TextAlignment="Center" IsReadOnly="True" Background="#2A2A30" Foreground="#888899" ToolTip="O limite aumenta conforme você sobe de Nível."/>
                    </Grid>

                    <CheckBox x:Name="ConfirmCmdCheckBox" Content="Confirmar comandos perigosos" Foreground="White" Margin="0,15,0,0"/>
                    <CheckBox x:Name="EphemeralSkillCheckBox" Content="Contexto efêmero de skills (economiza tokens)" Foreground="White" Margin="0,10,0,0"/>
                    
                    <TextBlock Text="Motor de Busca Web" Margin="0,15,0,5" Opacity="0.8"/>
                    <ComboBox x:Name="SearchEngineComboBox" Height="38">
                        <ComboBoxItem Content="DuckDuckGo"/>
                        <ComboBoxItem Content="Google"/>
                        <ComboBoxItem Content="Bing"/>
                    </ComboBox>
                    <TextBlock Text="DuckDuckGo não requer chave de API." FontSize="11" Opacity="0.4" Margin="0,2,0,0"/>
                    
                    <TextBlock Height="20"/>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancelar" Click="Cancel_Click" Width="80" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>
                <Button Content="Salvar" Click="Save_Click" Width="100" Height="35" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{StaticResource AiAccent}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
        </Border>
    </Border>
</Window>
